# scripts 目录说明

当前仓库里没有朋友教程对应的 `batch_upload.py`。

这里预留 `scripts/` 目录，是为了让后续补上传脚本时有一个明确位置：

- 期望路径：`scripts/batch_upload.py`
- `daily_scheduler.py` 会优先寻找这个位置

如果你之后把真正的上传脚本放进来，调度器就可以重新启用“渲染后自动上传”的流水线模式。
